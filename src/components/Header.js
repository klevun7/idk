import React from 'react';

const Header = () => {
    return (
        <header>
            <div className="text-3xl text-slate-600 font-sans"></div>
        </header>
    );
};

export default Header;